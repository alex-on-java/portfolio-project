apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: web-app-preview
  namespace: dev
spec:
  hostnames:
  - preview-web-app.dev.pr-16.portfolio-project.buyanov.de
  parentRefs:
  - name: portfolio-edge-gateway
    namespace: edge-system
    sectionName: https
  rules:
  - backendRefs:
    - name: web-app-preview
      port: 80
    matches:
    - path:
        type: PathPrefix
        value: /
