apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  labels:
    app: web-app
  name: web-app
  namespace: prd
spec:
  progressDeadlineSeconds: 120
  replicas: 1
  selector:
    matchLabels:
      app: web-app
  strategy:
    blueGreen:
      activeService: web-app-active
      autoPromotionEnabled: true
      previewService: web-app-preview
      scaleDownDelaySeconds: 35
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - image: ghcr.io/alex-on-java/web-app@sha256:28d322c73ce46d1962b6a2d7140c1317d89396bb0852a4a362828e57989a5ab5
        imagePullPolicy: IfNotPresent
        name: web-app
        ports:
        - containerPort: 3000
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          limits:
            cpu: 100m
            ephemeral-storage: 64Mi
            memory: 64Mi
          requests:
            cpu: 50m
            ephemeral-storage: 64Mi
            memory: 32Mi
